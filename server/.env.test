# =============================================================================
# StockFlow Server - Test Environment
# =============================================================================
# This file is used when running tests (jest loads it via dotenv)
# Uses a separate database to avoid affecting development data
# =============================================================================

# Database (Docker PostgreSQL - Test Database)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stockflow_test?schema=public"

# JWT (Tenant Users) - Test secrets
JWT_SECRET="test-jwt-secret"
JWT_REFRESH_SECRET="test-jwt-refresh-secret"
JWT_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

# JWT (System Admin) - Test secrets
SYSTEM_ADMIN_JWT_SECRET="test-system-admin-jwt-secret"
SYSTEM_ADMIN_JWT_REFRESH_SECRET="test-system-admin-refresh-secret"
SYSTEM_ADMIN_JWT_EXPIRATION="15m"
SYSTEM_ADMIN_JWT_REFRESH_EXPIRATION="7d"

# Brevo - Disabled for tests
BREVO_API_KEY=test-key
BREVO_SENDER_EMAIL=test@stockflow.com
BREVO_SENDER_NAME=StockFlow Test

# Admin
ADMIN_EMAIL=admin@stockflow.com
APP_URL=http://localhost:3000

# Arcjet - Disabled for tests
ARCJET_KEY=test-key
ARCJET_ENABLED=false

# Redis - Disabled for tests (use in-memory cache)
REDIS_HOST=
